# scenario: clean (golden reference)

The unmodified `esp-tflite-micro` hello_world sine model. Boots, runs TFLM,
prints the `y = sin(x)` sweep. Both checks pass.

This run is the trusted reference — `golden/sine.log` is captured from it.
